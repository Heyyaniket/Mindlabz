import 'package:flutter/material.dart';

class AppPallete {
  // From your palette: The main off-white background
  static const Color backgroundColor = Color(0xFFF9F9F7);

  // From your palette: The text/button black (Pure Black)
  static const Color black = Color(0xFF000000);

  // From your palette: The gold accent for "Create Account"
  static const Color goldAccent = Color(0xFFC9A761);

  // From your palette: The border color (used for Inputs and Social buttons)
  static const Color borderColor = Color(0xFFBEBDB9);

  // From your palette: Dark Grey for subheaders and labels
  static const Color textGrey = Color(0xFF4D4D4C);

  // From your palette: Pure White (likely for input fills if needed)
  static const Color white = Color(0xFFFFFFFF);
}